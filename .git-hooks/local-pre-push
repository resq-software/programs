#!/usr/bin/env bash
# Repo-specific pre-push — runs alongside the canonical ResQ pre-push hook.
set -euo pipefail

if command -v cargo >/dev/null 2>&1; then
    echo "  Anchor/Rust: cargo check --workspace"
    cargo check --workspace --quiet
fi
